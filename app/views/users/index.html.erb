<% provide(:title, 'All users') %>
<div class="signup">
	<div class="container">
<h1>Tutti gli utenti</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>

</div>
</div>