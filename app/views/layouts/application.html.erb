<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= full_title(yield(:title)) %></title>
    <meta name="description" content="Hai una band o sei un solista e vuoi registrare un album o una demo? Con una sala di registrazione adeguatamente insonorizzata e regia in locale separato abbiamo strumenti che ci permettono di avere risultati professionali. Offriamo servizi di mixing e mastering con ottimi prezzi e offerte."/>
    <meta name=”keywords” content="Studio registrazione, sala registrazione, project studio, studio professionale, polanesi, sori, genova, liguria,  offerte, promozioni, registrare, gruppo, band, musicale, ottimo suono, qualità audio eccellente, mixing, mastering, recording,registrare disco,incidere album, montaggio audio, montaggio video, videoclip, riprese, presa diretta, insonorizzazione"/>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>


    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->

    <!-- render post Facebook -->
    <div id="fb-root"></div>
    <script async defer crossorigin="anonymous" src="https://connect.facebook.net/it_IT/sdk.js#xfbml=1&version=v6.0"></script>
    <script src="https://kit.fontawesome.com/4ec30d7b7a.js" crossorigin="anonymous"></script>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css?family=Baloo+Paaji+2|Rock+Salt&display=swap" rel="stylesheet">

  </head>
  <body>
     <%= render 'layouts/header' %>
     <div class="<%= (:title) %>">
        <% flash.each do |message_type, message| %>
        <div class="container alert alert-<%= message_type %>"><%= message %></div>
        <% end %>
          <%= yield %>
            <%= render 'layouts/footer' %>
              <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
